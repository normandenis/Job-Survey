<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="jobtrends.job_aymax">

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity android:name=".SignInViewModel">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity android:name=".SignUpViewModel">
		</activity>
		<activity android:name=".NavDrawerViewModel">
		</activity>
		<activity android:name=".SettingViewModel">
		</activity>
		<activity android:name=".ProfileViewModel">
		</activity>
		<activity android:name=".StartSurveyViewModel">
		</activity>
		<activity
			android:name=".SurveyViewModel"
			android:label="@string/title_activity_survey_view_model"
			android:theme="@style/AppTheme">
		</activity>
	</application>

</manifest>