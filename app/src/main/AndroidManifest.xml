<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="jobtrends.job_aymax">

	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

	<application
		android:allowBackup="true"
		android:icon="@drawable/ic_logo_gray"
		android:label="@string/app_name"
		android:roundIcon="@drawable/ic_logo_gray"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity android:name=".viewmodel.SignInViewModel">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity android:name=".viewmodel.SignUpViewModel">
		</activity>
		<activity android:name=".viewmodel.HomeViewModel">
		</activity>
		<activity android:name=".viewmodel.SettingViewModel">
		</activity>
		<activity android:name=".viewmodel.ProfileViewModel">
		</activity>
		<!-- <activity android:name=".StartSurveyViewModel"> -->
		<!-- </activity> -->
		<!-- <activity android:name=".SurveyViewModel"> -->
		<!-- </activity> -->
		<!-- <activity android:name=".SurveyContentViewModel"> -->
		<!-- </activity> -->
		<activity android:name=".viewmodel.ListViewFAQViewModel">
		</activity>
		<activity android:name=".viewmodel.EndSurveyViewModel">
		</activity>
	</application>

</manifest>