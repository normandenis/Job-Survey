<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="jobtrends.jobaymax">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo_gray"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo_gray"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Set custom default icon. This is used when no icon is set for incoming notification messages.
             See README(https://goo.gl/l4GJaQ) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_logo_gray_500dp"/>
        <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent"/>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id"/>

        <activity android:name="jobtrends.jobaymax.viewmodel.SignInViewModel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="jobtrends.jobaymax.viewmodel.SignUpViewModel">
        </activity>
        <activity android:name="jobtrends.jobaymax.viewmodel.HomeViewModel">
        </activity>
        <!--<activity android:name=".viewmodel.SettingViewModel">-->
        <!--</activity>-->
        <!--<activity android:name=".viewmodel.ProfileViewModel">-->
        <!--</activity>-->
        <!-- <activity android:name=".StartSurveyViewModel"> -->
        <!-- </activity> -->
        <!-- <activity android:name=".SurveyViewModel"> -->
        <!-- </activity> -->
        <!-- <activity android:namQuestionViewModelodel"> -->
        <!-- </activity> -->
        <!--<activity android:name=".viewmodel.ListViewFAQViewModel">-->
        <!--</activity>-->
        <!--<activity android:name=".viewmodel.EndSurveyViewModel">-->
        <!--</activity>-->
        <!--<activity android:name=".viewmodel.NotificationPopupViewModel">-->
        <!--</activity>-->
        <!--<activity android:name=".viewmodel.CancelPopupViewModel">-->
        <!--</activity>-->
        <!--<activity android:name=".viewmodel.ValidatePopupViewModel">-->
        <!--</activity>-->

        <service android:name="jobtrends.jobaymax.service.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <service android:name="jobtrends.jobaymax.service.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>

    </application>

</manifest>