<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        tools:context="jobtrends.jobsurvey.viewmodel.AnswerViewModel">

  <data>
    <variable name="vm" type="jobtrends.jobsurvey.viewmodel.AnswerViewModel"/>
    <variable name="m" type="jobtrends.jobsurvey.model.Answer"/>
  </data>

  <LinearLayout android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

    <Button android:id="@+id/answer_button"
            android:layout_width="250dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="16dp"
            android:background="@drawable/outline_layout"
            android:onClick="@{(view) -> vm.onClick(m, view)}"
            android:text="@{m.title}"
            android:textAppearance="?android:attr/textAppearanceSmall"
            android:textColor="@android:color/black"
            android:textStyle="bold"/>

  </LinearLayout>

</layout>
